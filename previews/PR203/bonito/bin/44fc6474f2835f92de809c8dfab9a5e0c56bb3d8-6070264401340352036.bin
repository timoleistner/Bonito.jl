’Ç,j“Ù$447bfdab-b728-4e37-a410-2731745fba74€£sub‚§payload’Èk’Ç;j“Ù$447bfdab-b728-4e37-a410-2731745fba74¤1430×e’¤1430¡1£subƒ£obsÇh¤1430§payloadÇ¡f“€ÙVreturn (val)=> {
    document.querySelector('[data-jscall-id="21"]').innerText = val
}ÙE/home/runner/work/Bonito.jl/Bonito.jl/src/rendering/observables.jl:86¨msg_type¡1ÈUk’ÇTj“Ù$447bfdab-b728-4e37-a410-2731745fba74‚¤1428Çe’¤1428š	
¤1429Çe’¤1429£sub‚§payloadÈåf“‚´18018592370426883457Çh¤1429³9530064358432116345Çh¤1428Új((container)=> {
    const thumb = document.querySelector('[data-jscall-id="19"]');
    const track_active = document.querySelector('[data-jscall-id="18"]');
    const track = document.querySelector('[data-jscall-id="17"]');
    let isDragging = false;
    function set_thumb(e) {
        const values = __lookup_interpolated('9530064358432116345').value;
        const nsteps = values.length;
        const thumb_width = thumb.offsetWidth / 2;
        const width = track.offsetWidth;
        const step_width = width / nsteps;
        let new_left = e.clientX - container.getBoundingClientRect().left;
        new_left = Math.max(new_left, 0);
        new_left = Math.min(new_left, width);
        new_left = Math.round(new_left / step_width) * step_width;
        thumb.style.left = (new_left - thumb_width) + 'px';  // Update the left position of the thumb
        track_active.style.width = new_left + 'px';  // Update the active track
        const index = Math.round((new_left / width) * (nsteps - 1));
        __lookup_interpolated('18018592370426883457').notify(values[index]);
    }
    const controller = new AbortController();
    document.addEventListener('mousedown', function (e) {
        if(e.target === thumb || e.target === track_active || e.target === track || e.targar === container){
            isDragging = true;
            set_thumb(e);
            e.preventDefault();  // Prevent default behavior
        }
    }, { signal: controller.signal});
    document.addEventListener('mouseup', function () {
        if (!document.body.contains(container)) {
            controller.abort();
        }
        isDragging = false;
    }, { signal: controller.signal });
    document.addEventListener('mousemove', function (e) {
        if (isDragging) {
            set_thumb(e);
        }
    }, { signal: controller.signal });
}
)(document.querySelector('[data-jscall-id="16"]'));Ù;/home/runner/work/Bonito.jl/Bonito.jl/src/components.jl:333¨msg_type¡2¨msg_type¡9